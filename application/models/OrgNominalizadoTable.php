<?php

/**
 * OrgNominalizado
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 * 
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 7490 2010-03-29 19:53:27Z jwage $
 */
class OrgNominalizadoTable extends Doctrine_Table {

    /**
     * Returns an instance of this class.
     *
     * @return OrgNominalizadoTable
     */
    public static function getInstance() {
        return Doctrine_Core::getTable('OrgNominalizado');
    }

    /** Devuelve todos los OrgNominalizados
     * 
     * @return type
     */
    public function findAllWithEntidadProducto($mes, $year) {
        if (!$year || $year === '%') {
            $year = date("Y");
        }
        if (!$mes || $mes === '%') {
            $mes = date("m");
        }
        $temp = $year . '-' . $mes . '%';
        return $this->createQuery('org_nominalizado')
                        ->select('org_nominalizado.*, pro.nombre as producto_nombre'
                                . ',ent.nombre as entidad_nombre,prov.nombre as proveedor')
                        ->innerJoin('org_nominalizado.Producto pro')
                        ->innerJoin('pro.Proveedor prov')
                        ->innerJoin('org_nominalizado.Entidad ent')
                        ->where('org_nominalizado.fecha like "' . $temp . '"')
                        ->orderBy('producto_nombre')
                        ->fetchArray();
    }

    public function findByEntidadId($id, $mes, $year) {
        if (!$year || $year === '%') {
            $year = date("Y");
        }
        if (!$mes || $mes === '%') {
            $mes = date("m");
        }
        $temp = $year . '-' . $mes . '%';
        return $this->createQuery('org_nominalizado')
                        ->select('org_nominalizado.*,ent.*,pro.*, pro.nombre as producto_nombre')
                        ->innerJoin('org_nominalizado.Entidad ent')
                        ->innerJoin('org_nominalizado.Producto pro')
                        ->where('ent.id = ?', $id)
                        ->andWhere('org_nominalizado.fecha like "' . $temp . '"')
                        ->fetchArray();
    }

}
